# Copy to .env and fill values for local development
# MySQL settings (used by backend/database.js)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=login_scrum
DB_PORT=3306

# App
PORT=3000
JWT_SECRET=una_clave_muy_secreta_y_larga
